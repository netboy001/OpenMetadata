[build-system]
requires = ["setuptools~=70.3.0"]
build-backend = "setuptools.build_meta"

# We will keep handling dependencies in setup.py
# since it helps us organize and isolate version management
[project]
name = "umetadata_managed_apis"
version = "1.12.14.0"
readme = "README.md"
authors = [
    {name = "UMetadata Committers"}
]
license = {file = "LICENSE"}
description = "Airflow REST APIs to create and manage DAGS"
requires-python = ">=3.9"
dependencies = [
    "pendulum~=3.0",
    "apache-airflow>=2.2.2",
    "apache-airflow-providers-fab>=1.0.0",
    "Flask==2.2.5",
    "Flask-Admin==1.6.0",
    "packaging>=20.0",
]

[project.optional-dependencies]
dev = [
    "black==22.3.0",
    "pytest",
    "pylint",
    "pytest-cov",
    "isort",
    "pycln",
]

[project.urls]
Homepage = "https://wondersgroup.com/"
Documentation = "https://docs.wondersgroup.com/"
Source = "https://github.com/u-metadata/UMetadata"


[tool.setuptools.packages.find]
include = ["umetadata_managed_apis.*", "umetadata_managed_apis"]

[tool.setuptools.package-data]
"umetadata_managed_apis" = ["views/templates/rest_api/index.html", "resources/dag_runner.j2"]

[project.entry-points."airflow.plugins"]
umetadata_managed_apis = "umetadata_managed_apis.plugin:RestApiPlugin"


[tool.coverage.run]
source = [
    "env/lib/python3.9/site-packages/umetadata_managed_apis"
]
relative_files = true
branch = true

[tool.coverage.report]
omit = [
    "*__init__*",
    "tests/*",
    "views/**",
    "plugin.py"
]