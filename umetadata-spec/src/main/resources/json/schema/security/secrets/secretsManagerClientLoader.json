{
  "$id": "https://u-metadata.org/schema/security/secrets/secretsManagerClientLoader.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Secrets Manager Client Loader",
  "description": "UMetadata Secrets Manager Client Loader. Lets the client know how the Secrets Manager Credentials should be loaded from the environment.",
  "type": "string",
  "javaType": "org.umetadata.schema.security.secrets.SecretsManagerClientLoader",
  "enum": ["noop", "airflow","env"],
  "default": "noop",
  "additionalProperties": false
}
