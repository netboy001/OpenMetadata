email:
  emailingEntity: ${OM_EMAIL_ENTITY:-"UMetadata"}
  supportUrl: ${OM_SUPPORT_URL:-"https://slack.wondersgroup.com"}
  enableSmtpServer : ${AUTHORIZER_ENABLE_SMTP:-false}
  senderMail: ${UMETADATA_SMTP_SENDER_MAIL:-"sender@gmail.com"}
  serverEndpoint: ${SMTP_SERVER_ENDPOINT:-"smtp.gmail.com"}
  serverPort: ${SMTP_SERVER_PORT:-"587"}
  username: ${SMTP_SERVER_USERNAME:-""}
  password: ${SMTP_SERVER_PWD:-""}
  transportationStrategy: ${SMTP_SERVER_STRATEGY:-"SMTP_TLS"}
  templates: ${TEMPLATES:-"umetadata"}

serverUrl:
  uMetadataUrl: ${UMETADATA_SERVER_URL:-"http://localhost:8585"}
  
